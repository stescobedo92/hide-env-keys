# evault-cli platform packages

These packages are published as optional dependencies of `evault-cli`.
Each package contains one native `evault` binary and is constrained with
`os` and `cpu` fields so npm installs only the package matching the user's
platform.
